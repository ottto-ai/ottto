### ottto --help
Ottto local platform CLI for developers, support, CI, and AI agents

Usage: ottto [OPTIONS] <COMMAND>

Commands:
  status           Show daemon, account, relay, update, and app health
  apps             List apps or refresh app setup status
  agent-status     Refresh one source status using the lower-level source noun
  context          Print cloud context for AI agents
  costs            Print cloud cost breakdown for AI agents
  cloud            Plan, register, test, sync, or inspect cloud connectors
  sessions         Print cloud sessions for AI agents
  recommendations  Print Advisor recommendations for AI agents
  provider-impact  Print staff-only provider-impact events for AI agents
  setup            Connect this Mac through a browser claim
  login            Sign in and connect this Mac through a browser claim
  account          Show the Ottto account connected to this Mac
  logout           Disconnect this Mac from Ottto
  doctor           Run daemon health checks and print current status
  fix              Apply daemon-approved repair for one app
  verify           Verify one app and publish safe setup status
  config           Show or persist local settings that survive upgrades
  diagnostics      Collect local-only or approved support diagnostics
  update           Check owner-aware update state and instructions
  uninstall        Remove Ottto local runtime state for this user
  help             Print this message or the help of the given subcommand(s)

Options:
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto status --help
Show daemon, account, relay, update, and app health

Usage: ottto status [OPTIONS]

Options:
      --json                  Print one final JSON object and no human summary text
      --socket <SOCKET>       Override the ottto-service Unix socket path
      --refresh-agent-status  Refresh Codex, Claude Code, and Pi status before returning
      --token <TOKEN>         Override the local-control token for CLI and agent requests
      --no-autostart          Do not kickstart the standard per-user ottto-service
      --watch                 Emit NDJSON progress events and a final event; requires --json
  -h, --help                  Print help
### ottto apps --help
List apps or refresh app setup status

Usage: ottto apps [OPTIONS] [COMMAND]

Commands:
  detect  Refresh all supported app statuses
  status  Refresh and return one app status
  help    Print this message or the help of the given subcommand(s)

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto apps detect --help
Refresh all supported app statuses

Usage: ottto apps detect [OPTIONS]

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto apps status --help
Refresh and return one app status

Usage: ottto apps status [OPTIONS] --app <APP>

Options:
      --app <APP>        App to refresh [possible values: codex, claude-code, pi]
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --json             Print one final JSON object and no human summary text
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto agent-status --help
Refresh one source status using the lower-level source noun

Usage: ottto agent-status [OPTIONS] <--source <SOURCE>|--app <APP>>

Options:
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --source <SOURCE>  Source to operate on [possible values: codex, claude-code, pi]
      --app <APP>        App to operate on [possible values: codex, claude-code, pi]
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --json             Print one final JSON object and no human summary text
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto context --help
Print cloud context for AI agents

Usage: ottto context [OPTIONS]

Options:
      --json
          Print one final JSON object and no human summary text
      --socket <SOCKET>
          Override the ottto-service Unix socket path
      --days <DAYS>
          Number of days to include
      --token <TOKEN>
          Override the local-control token for CLI and agent requests
      --no-autostart
          Do not kickstart the standard per-user ottto-service
      --range <RANGE>
          Calendar range preset
      --start-date <YYYY-MM-DD>
          Inclusive custom window start date
      --watch
          Emit NDJSON progress events and a final event; requires --json
      --end-date <YYYY-MM-DD>
          Inclusive custom window end date
      --timezone <TZ>
          IANA timezone for calendar windows
      --source <SOURCE>
          Source slug to filter context to
      --app <APP>
          App/source alias to filter context to [possible values: codex, claude-code, pi]
      --machine-id <MACHINE_ID>
          Override the local machine filter
      --source-plan-profile-id <PROFILE_ID>
          Filter by source plan profile id
      --max-tokens <TOKENS>
          Approximate output token budget
      --all-machines
          Request account-wide context instead of this Mac
  -h, --help
          Print help
### ottto costs --help
Print cloud cost breakdown for AI agents

Usage: ottto costs [OPTIONS]

Options:
      --json
          Print one final JSON object and no human summary text
      --socket <SOCKET>
          Override the ottto-service Unix socket path
      --days <DAYS>
          Number of days to include
      --token <TOKEN>
          Override the local-control token for CLI and agent requests
      --no-autostart
          Do not kickstart the standard per-user ottto-service
      --range <RANGE>
          Calendar range preset
      --start-date <YYYY-MM-DD>
          Inclusive custom window start date
      --watch
          Emit NDJSON progress events and a final event; requires --json
      --end-date <YYYY-MM-DD>
          Inclusive custom window end date
      --timezone <TZ>
          IANA timezone for calendar windows
      --source <SOURCE>
          Source slug to filter costs to
      --app <APP>
          App/source alias to filter costs to [possible values: codex, claude-code, pi]
      --machine-id <MACHINE_ID>
          Override the local machine filter
      --source-plan-profile-id <PROFILE_ID>
          Filter by source plan profile id
      --bucket <BUCKET>
          Cost bucket such as day or hour
      --mode <MODE>
          Breakdown mode such as full or overview
      --all-machines
          Request account-wide costs instead of this Mac
  -h, --help
          Print help
### ottto cloud --help
Plan, register, test, sync, or inspect cloud connectors

Usage: ottto cloud [OPTIONS] <COMMAND>

Commands:
  materials  Show provider-side setup commands; makes no changes
  plan       Preview a keyless connector manifest; makes no changes
  register   Save a tested keyless connector manifest
  test       Test supplied or already-saved connector settings
  sync       Run an explicitly approved cloud billing sync
  status     Show configured cloud connector status; makes no changes
  help       Print this message or the help of the given subcommand(s)

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto cloud materials --help
Show provider-side setup commands; makes no changes

Usage: ottto cloud materials [OPTIONS] --source <SOURCE>

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --source <SOURCE>  [possible values: bedrock, vertex]
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto cloud plan --help
Preview a keyless connector manifest; makes no changes

Usage: ottto cloud plan [OPTIONS] --source <SOURCE> --config-file <PATH>

Options:
      --json                Print one final JSON object and no human summary text
      --socket <SOCKET>     Override the ottto-service Unix socket path
      --source <SOURCE>     [possible values: bedrock, vertex]
      --config-file <PATH>  Keyless connector JSON manifest
      --token <TOKEN>       Override the local-control token for CLI and agent requests
      --no-autostart        Do not kickstart the standard per-user ottto-service
      --watch               Emit NDJSON progress events and a final event; requires --json
  -h, --help                Print help
### ottto cloud register --help
Save a tested keyless connector manifest

Usage: ottto cloud register [OPTIONS] --source <SOURCE> --config-file <PATH>

Options:
      --json                Print one final JSON object and no human summary text
      --socket <SOCKET>     Override the ottto-service Unix socket path
      --source <SOURCE>     [possible values: bedrock, vertex]
      --config-file <PATH>  Keyless connector JSON manifest
      --token <TOKEN>       Override the local-control token for CLI and agent requests
      --approve             Approve saving this connector configuration
      --no-autostart        Do not kickstart the standard per-user ottto-service
      --watch               Emit NDJSON progress events and a final event; requires --json
  -h, --help                Print help
### ottto cloud test --help
Test supplied or already-saved connector settings

Usage: ottto cloud test [OPTIONS] --source <SOURCE>

Options:
      --json                Print one final JSON object and no human summary text
      --socket <SOCKET>     Override the ottto-service Unix socket path
      --source <SOURCE>     [possible values: bedrock, vertex]
      --config-file <PATH>  Optional keyless connector JSON manifest
      --token <TOKEN>       Override the local-control token for CLI and agent requests
      --no-autostart        Do not kickstart the standard per-user ottto-service
      --watch               Emit NDJSON progress events and a final event; requires --json
  -h, --help                Print help
### ottto cloud sync --help
Run an explicitly approved cloud billing sync

Usage: ottto cloud sync [OPTIONS] --source <SOURCE>

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --source <SOURCE>  [possible values: bedrock, vertex]
      --days <DAYS>      [default: 1]
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --approve          Approve starting this billable provider query
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto cloud status --help
Show configured cloud connector status; makes no changes

Usage: ottto cloud status [OPTIONS]

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --source <SOURCE>  [possible values: bedrock, vertex]
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto sessions --help
Print cloud sessions for AI agents

Usage: ottto sessions [OPTIONS]

Options:
      --json
          Print one final JSON object and no human summary text
      --socket <SOCKET>
          Override the ottto-service Unix socket path
      --limit <LIMIT>
          Maximum number of sessions to return
      --token <TOKEN>
          Override the local-control token for CLI and agent requests
      --cursor <CURSOR>
          Pagination cursor from a prior response
      --no-autostart
          Do not kickstart the standard per-user ottto-service
      --range <RANGE>
          Calendar range preset
      --watch
          Emit NDJSON progress events and a final event; requires --json
      --start-date <YYYY-MM-DD>
          Inclusive custom window start date
      --end-date <YYYY-MM-DD>
          Inclusive custom window end date
      --timezone <TZ>
          IANA timezone for calendar windows
      --source <SOURCE>
          Source slug to filter sessions to
      --app <APP>
          App/source alias to filter sessions to [possible values: codex, claude-code, pi]
      --model <MODEL>
          Model name filter
      --billing-provider <PROVIDER>
          Billing provider filter
      --billing-channel <CHANNEL>
          Billing channel filter
      --machine-id <MACHINE_ID>
          Override the local machine filter
      --source-plan-profile-id <PROFILE_ID>
          Filter by source plan profile id
      --min-cost <USD>
          Minimum session cost
      --max-cost <USD>
          Maximum session cost
      --sort-by <FIELD>
          Sort field
      --sort-dir <DIR>
          Sort direction, asc or desc
      --search <TEXT>
          Search text
      --all-machines
          Request account-wide sessions instead of this Mac
  -h, --help
          Print help
### ottto recommendations --help
Print Advisor recommendations for AI agents

Usage: ottto recommendations [OPTIONS]

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto provider-impact --help
Print staff-only provider-impact events for AI agents

Usage: ottto provider-impact [OPTIONS]

Options:
      --json                        Print one final JSON object and no human summary text
      --socket <SOCKET>             Override the ottto-service Unix socket path
      --date-from <YYYY-MM-DD>      Inclusive event date start
      --token <TOKEN>               Override the local-control token for CLI and agent requests
      --date-to <YYYY-MM-DD>        Inclusive event date end
      --no-autostart                Do not kickstart the standard per-user ottto-service
      --provider <PROVIDER>         Provider filter
      --watch                       Emit NDJSON progress events and a final event; requires --json
      --app <APP>                   Provider app or surface filter
      --kind <KIND>                 Provider-impact kind filter
      --confidence <CONFIDENCE>     Confidence filter
      --impact-priority <PRIORITY>  Impact priority filter
      --status <STATUS>             Review status filter
      --q <TEXT>                    Search text
      --limit <LIMIT>               Maximum number of events to return
  -h, --help                        Print help
### ottto setup --help
Connect this Mac through a browser claim

Usage: ottto setup [OPTIONS]

Options:
      --claim-code <CLAIM_CODE>  Browser claim code from the Ottto Apps page
      --socket <SOCKET>          Override the ottto-service Unix socket path
      --no-browser               Do not open the browser; print the claim URL and code
      --token <TOKEN>            Override the local-control token for CLI and agent requests
      --no-autostart             Do not kickstart the standard per-user ottto-service
      --no-wait                  Return after starting or attaching setup without waiting
      --timeout <SECONDS>        Seconds to wait for browser approval and setup progress [default: 300]
      --watch                    Emit NDJSON progress events and a final event; requires --json
      --json                     Print one final JSON object and no human summary text
  -h, --help                     Print help
### ottto login --help
Sign in and connect this Mac through a browser claim

Usage: ottto login [OPTIONS]

Options:
      --claim-code <CLAIM_CODE>  Browser claim code from the Ottto Apps page
      --socket <SOCKET>          Override the ottto-service Unix socket path
      --no-browser               Do not open the browser; print the claim URL and code
      --token <TOKEN>            Override the local-control token for CLI and agent requests
      --no-autostart             Do not kickstart the standard per-user ottto-service
      --no-wait                  Return after starting or attaching setup without waiting
      --timeout <SECONDS>        Seconds to wait for browser approval and setup progress [default: 300]
      --watch                    Emit NDJSON progress events and a final event; requires --json
      --json                     Print one final JSON object and no human summary text
  -h, --help                     Print help
### ottto account --help
Show the Ottto account connected to this Mac

Usage: ottto account [OPTIONS]

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto logout --help
Disconnect this Mac from Ottto

Usage: ottto logout [OPTIONS]

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --local-only       Clear local credentials without first disconnecting this Mac in Ottto
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto doctor --help
Run daemon health checks and print current status

Usage: ottto doctor [OPTIONS]

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto fix --help
Apply daemon-approved repair for one app

Usage: ottto fix [OPTIONS] <--source <SOURCE>|--app <APP>>

Options:
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --source <SOURCE>  Source to operate on [possible values: codex, claude-code, pi]
      --app <APP>        App to operate on [possible values: codex, claude-code, pi]
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --json             Print one final JSON object and no human summary text
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto verify --help
Verify one app and publish safe setup status

Usage: ottto verify [OPTIONS] <--source <SOURCE>|--app <APP>>

Options:
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --source <SOURCE>  Source to operate on [possible values: codex, claude-code, pi]
      --app <APP>        App to operate on [possible values: codex, claude-code, pi]
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --repair           Repair local telemetry config drift before running verification
      --json             Print one final JSON object and no human summary text
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto config --help
Show or persist local settings that survive upgrades

Usage: ottto config [OPTIONS] <COMMAND>

Commands:
  show  Show local settings and how each one resolves here
  set   Persist one local setting so upgrades keep it
  help  Print this message or the help of the given subcommand(s)

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto config show --help
Show local settings and how each one resolves here

Usage: ottto config show [OPTIONS]

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto config set --help
Persist one local setting so upgrades keep it

Usage: ottto config set [OPTIONS] --setting <SETTING> --value <VALUE>

Options:
      --json               Print one final JSON object and no human summary text
      --setting <SETTING>  Setting to persist [possible values: claude-attribution-capture]
      --socket <SOCKET>    Override the ottto-service Unix socket path
      --token <TOKEN>      Override the local-control token for CLI and agent requests
      --value <VALUE>      Value to persist [possible values: on, off]
      --no-autostart       Do not kickstart the standard per-user ottto-service
      --watch              Emit NDJSON progress events and a final event; requires --json
  -h, --help               Print help
### ottto diagnostics --help
Collect local-only or approved support diagnostics

Usage: ottto diagnostics [OPTIONS] <COMMAND>

Commands:
  collect  Collect a redacted diagnostics bundle
  help     Print this message or the help of the given subcommand(s)

Options:
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto diagnostics collect --help
Collect a redacted diagnostics bundle

Usage: ottto diagnostics collect [OPTIONS]

Options:
      --json                           Print one final JSON object and no human summary text
      --socket <SOCKET>                Override the ottto-service Unix socket path
      --token <TOKEN>                  Override the local-control token for CLI and agent requests
      --upload                         Request backend diagnostics upload after collection
      --approve-upload                 Confirm diagnostics upload approval
      --no-autostart                   Do not kickstart the standard per-user ottto-service
      --accept-retention-disclosure    Accept the support retention disclosure
      --watch                          Emit NDJSON progress events and a final event; requires --json
      --support-claim <SUPPORT_CLAIM>  Support claim authorizing upload
  -h, --help                           Print help
### ottto update --help
Check owner-aware update state and instructions

Usage: ottto update [OPTIONS] [COMMAND]

Commands:
  check  Check the release manifest without applying an update
  help   Print this message or the help of the given subcommand(s)

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto update check --help
Check the release manifest without applying an update

Usage: ottto update check [OPTIONS]

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
### ottto uninstall --help
Remove Ottto local runtime state for this user

Usage: ottto uninstall [OPTIONS]

Options:
      --json             Print one final JSON object and no human summary text
      --socket <SOCKET>  Override the ottto-service Unix socket path
      --token <TOKEN>    Override the local-control token for CLI and agent requests
      --no-autostart     Do not kickstart the standard per-user ottto-service
      --watch            Emit NDJSON progress events and a final event; requires --json
  -h, --help             Print help
